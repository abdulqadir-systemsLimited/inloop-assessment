import React from 'react';
import { View, Text } from 'react-native';

const EndOfResult: React.FC = () => {
  return (
    <View style={{ alignItems: 'center', marginTop: 20 }}>
      <Text>No more gifs available</Text>
    </View>
  );
};

export {EndOfResult};
